//REDUx State Management.



const initState = {
    identity:"",
    isHost:"",
}

const reducer = (state = initState , action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default reducer